# Clase Fake Python - Instrucciones de Instalación

Para instalar esta aplicación, sigue los pasos a continuación:

1. Abre la línea de comandos y ejecuta el archivo `install.bat` utilizando PowerShell:
   ```powershell
   ./install.bat
2 Luego ejecutar :
    ./boot.bat
